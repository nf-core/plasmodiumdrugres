nextflow_process {

    name "Test Process EXTRACT_PANEL_INFO_TO_BED"
    script "modules/local/extract_panel_info_to_bed"
    process "EXTRACT_PANEL_INFO_TO_BED"

    test("Should extract panel info without failures") {

        when {
            process {
                """
                input[0] = file("${projectDir}/tests/input/example_PMO.json")
                input[1] = "FALSE"
                """
            }
        }

        then {
            assert process.success
            assert process.out.panel_info_bed != null
            assert path(process.out.panel_info_bed.get(0)).md5 == "df63a89d3487c1d81e38aa1abab2240d"
        }

    }

    test("Should extract panel info with add_ref_seqs without failures") {

        when {
            process {
                """
                input[0] = file("${projectDir}/tests/input/example_PMO.json")
                input[1] = "TRUE"
                """
            }
        }

        then {
            assert process.success
            assert process.out.panel_info_bed != null
            assert path(process.out.panel_info_bed.get(0)).md5 == "5655fad30ea541867780d735327e67fe"
        }

    }

}
