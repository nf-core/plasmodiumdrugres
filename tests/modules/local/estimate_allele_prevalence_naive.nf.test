nextflow_process {

    name "Test Process ESTIMATE_ALLELE_PREVALENCE_NAIVE"
    script "modules/local/estimate_allele_prevalence_naive"
    process "ESTIMATE_ALLELE_PREVALENCE_NAIVE"

    test("Estimate allele prev should run without failures") {

        when {
            process {
                """
                input[0] = file("${projectDir}/tests/input/example_amino_acid_calls.tsv")
                """
            }
        }

        then {
            assert process.success
            // channel exists
            assert process.out.allele_prevalence != null

            // unpack tuple output
            def (pop, outFile) = process.out.allele_prevalence.get(0)

            assert pop == 'example_amino_acid_calls'

            // output md5 check
            assert path(outFile).md5 == "716a7d5e590b87ee9189857145eb4b37"
        }

    }

}
